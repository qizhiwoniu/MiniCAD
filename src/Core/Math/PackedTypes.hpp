#pragma once
#include <cstdint>

namespace MiniCAD::Math
{
    struct Float2     { float x, y; };
    struct Float3     { float x, y, z; };
    struct Float4     { float x, y, z, w; };
    struct UByte4Norm { uint8_t r, g, b, a; };
}
